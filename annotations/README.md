# QA Annotation Tool

## Setup

```bash
pip install flask requests
```

## Usage

```bash
# From project root (cs288-sp26-a3/)
python scripts/annotate_app.py annotations/iaa_annotator1_template.jsonl
```

Optional: `--port 5051` if port 5050 is taken.

Open http://localhost:5050 in your browser. Progress saves automatically.
